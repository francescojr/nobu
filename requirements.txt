# Core (MCP compose + render scripts)
fastmcp>=2.0
midiutil>=1.2
mido>=1.3
numpy>=1.26
soundfile>=0.12

# Optional: hybrid SF2 drums + chiptune melodic (scripts/render_track.py)
# tinysoundfont==0.3.7

# System tools (not pip):
#   FluidSynth  — full SF2 render to WAV (scripts/render_* --mode sf2)
#   ffmpeg      — WAV → OGG (recommended on Windows; render_track + SF2 path)
